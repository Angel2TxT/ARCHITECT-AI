# Rutas relativas al directorio plano-validador/
path: datasets/cubicasa_yolo
train: images/train
val: images/val

# Dataset CubiCasa (histórico, 4 clases). Para ResPlan ampliado usa config/data_resplan.yaml
path: datasets/cubicasa_yolo
train: images/train
val: images/val

# Clases del best.pt CubiCasa. Tras entrenar ResPlan, apunta Ajustes a
# runs/detect/plano_resplan_s/weights/best.pt (8 clases).
#
# Inferencia: tiles + imgsz adaptativo (core/tiled_infer.py).
# Dataset profesional abierto: scripts/resplan_to_yolo.py (CC BY 4.0).
nc: 4
names:
  0: door
  1: window
  2: wall
  3: room

# Ampliadas en ResPlan (data_resplan.yaml):
# 4: stair  5: bathroom  6: kitchen  7: parking
